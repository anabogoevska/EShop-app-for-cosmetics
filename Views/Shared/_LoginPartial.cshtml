@using Microsoft.AspNetCore.Identity
@inject SignInManager<EShop.Domain.Identity.EShopApplicationUser> SignInManager
@inject UserManager<EShop.Domain.Identity.EShopApplicationUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <a class="nav-link " style="color: white;" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage"><b>Hello @User.Identity.Name!</b></a>
        </li>
        <li class="nav-item">
            @* <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
                </form> *@
            <a class="nav-link " style="color:white;" asp-controller="Account" asp-action="Logout"><b>Logout</b></a>
        </li>
    }
    else
    {
        <li class="nav-item" style=" margin-top: 11px;">
            <a class="nav-link " style="color: white;" asp-controller="Account" asp-action="Register"> <b>Register</b></a>
        </li>
        <li class="nav-item" style=" margin-top: 11px;">

            <a class="nav-link " style="color: white;" asp-area="" asp-controller="Account" asp-action="Login"><b>Login</b></a>
        </li>
       
    }
</ul>
